---
title: listById
---

import Details from '@components/details.astro';
import Tag from '@components/tag.astro';

<Tag apiPath={'/v4/lists/:id'} />

This method returns a single list record. For official documentation of the
[endpoint](https://thetvdb.github.io/v4-api/#/Lists/getList), please refer to the Swagger
documentation.

## Parameters

| Params | Type     | Required | Description   |
| ------ | -------- | :------: | ------------- |
| id     | `string` |   Yes    | The list `id` |

## Example

```js {4}
import { TheTVDB } from '@untidy/thetvdb';

const client = new TheTVDB('access token');
const data = await client.listById('1');
```

<Details>

```js
{
  status: 'success',
  data: {
    id: 1,
    name: 'Scooby-Doo',
    overview:
      'The following is a list of the various media from the Scooby-Doo franchise which includes series, films and specials.',
    url: '1001',
    // ...
  },
};
```

</Details>
